# Unifapi::SeoBacklinksBulkBacklinksResponse

## Properties

| Name | Type | Description | Notes |
| ---- | ---- | ----------- | ----- |
| **results** | [**Array&lt;SeoBacklinksBulkBacklinkItem&gt;**](SeoBacklinksBulkBacklinkItem.md) | Backlink count for each requested target. Each target is one billable record. |  |

## Example

```ruby
require 'unifapi'

instance = Unifapi::SeoBacklinksBulkBacklinksResponse.new(
  results: null
)
```

