=begin
#UnifAPI

#Unified, AI-friendly API for public-data providers. Single source of truth for the UnifAPI gateway — consumed by docs (Fumadocs), the marketing site, and downstream SDK generators.

The version of the OpenAPI document: 0.0.1
Contact: support@unifapi.com
Generated by: https://openapi-generator.tech
Generator version: 7.10.0

=end

require 'date'
require 'time'

module Unifapi
  class BrowserScreenshotRequest
    # Absolute http(s) URL of the page to render.
    attr_accessor :url

    # When navigation is considered complete. `networkidle0` waits for the network to go idle (best for JavaScript-heavy pages); `load` is fastest.
    attr_accessor :wait_until

    # Navigation timeout in milliseconds (1000–60000).
    attr_accessor :timeout_ms

    attr_accessor :viewport

    # Capture the full scrollable page instead of just the viewport.
    attr_accessor :full_page

    # Output image format.
    attr_accessor :format

    # Compression quality (0–100). Only valid for `jpeg` and `webp`.
    attr_accessor :quality

    # Capture only the first element matching this CSS selector.
    attr_accessor :selector

    # Render a transparent background (ignored for `jpeg`).
    attr_accessor :omit_background

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'url' => :'url',
        :'wait_until' => :'wait_until',
        :'timeout_ms' => :'timeout_ms',
        :'viewport' => :'viewport',
        :'full_page' => :'full_page',
        :'format' => :'format',
        :'quality' => :'quality',
        :'selector' => :'selector',
        :'omit_background' => :'omit_background'
      }
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'url' => :'String',
        :'wait_until' => :'String',
        :'timeout_ms' => :'Integer',
        :'viewport' => :'BrowserScreenshotRequestViewport',
        :'full_page' => :'Boolean',
        :'format' => :'String',
        :'quality' => :'Integer',
        :'selector' => :'String',
        :'omit_background' => :'Boolean'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `Unifapi::BrowserScreenshotRequest` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `Unifapi::BrowserScreenshotRequest`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'url')
        self.url = attributes[:'url']
      else
        self.url = nil
      end

      if attributes.key?(:'wait_until')
        self.wait_until = attributes[:'wait_until']
      end

      if attributes.key?(:'timeout_ms')
        self.timeout_ms = attributes[:'timeout_ms']
      end

      if attributes.key?(:'viewport')
        self.viewport = attributes[:'viewport']
      end

      if attributes.key?(:'full_page')
        self.full_page = attributes[:'full_page']
      end

      if attributes.key?(:'format')
        self.format = attributes[:'format']
      else
        self.format = 'png'
      end

      if attributes.key?(:'quality')
        self.quality = attributes[:'quality']
      end

      if attributes.key?(:'selector')
        self.selector = attributes[:'selector']
      end

      if attributes.key?(:'omit_background')
        self.omit_background = attributes[:'omit_background']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      warn '[DEPRECATED] the `list_invalid_properties` method is obsolete'
      invalid_properties = Array.new
      if @url.nil?
        invalid_properties.push('invalid value for "url", url cannot be nil.')
      end

      if @url.to_s.length < 1
        invalid_properties.push('invalid value for "url", the character length must be great than or equal to 1.')
      end

      if !@timeout_ms.nil? && @timeout_ms > 60000
        invalid_properties.push('invalid value for "timeout_ms", must be smaller than or equal to 60000.')
      end

      if !@timeout_ms.nil? && @timeout_ms < 1000
        invalid_properties.push('invalid value for "timeout_ms", must be greater than or equal to 1000.')
      end

      if !@quality.nil? && @quality > 100
        invalid_properties.push('invalid value for "quality", must be smaller than or equal to 100.')
      end

      if !@quality.nil? && @quality < 0
        invalid_properties.push('invalid value for "quality", must be greater than or equal to 0.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      warn '[DEPRECATED] the `valid?` method is obsolete'
      return false if @url.nil?
      return false if @url.to_s.length < 1
      wait_until_validator = EnumAttributeValidator.new('String', ["load", "domcontentloaded", "networkidle0", "networkidle2"])
      return false unless wait_until_validator.valid?(@wait_until)
      return false if !@timeout_ms.nil? && @timeout_ms > 60000
      return false if !@timeout_ms.nil? && @timeout_ms < 1000
      format_validator = EnumAttributeValidator.new('String', ["png", "jpeg", "webp"])
      return false unless format_validator.valid?(@format)
      return false if !@quality.nil? && @quality > 100
      return false if !@quality.nil? && @quality < 0
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] url Value to be assigned
    def url=(url)
      if url.nil?
        fail ArgumentError, 'url cannot be nil'
      end

      if url.to_s.length < 1
        fail ArgumentError, 'invalid value for "url", the character length must be great than or equal to 1.'
      end

      @url = url
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] wait_until Object to be assigned
    def wait_until=(wait_until)
      validator = EnumAttributeValidator.new('String', ["load", "domcontentloaded", "networkidle0", "networkidle2"])
      unless validator.valid?(wait_until)
        fail ArgumentError, "invalid value for \"wait_until\", must be one of #{validator.allowable_values}."
      end
      @wait_until = wait_until
    end

    # Custom attribute writer method with validation
    # @param [Object] timeout_ms Value to be assigned
    def timeout_ms=(timeout_ms)
      if timeout_ms.nil?
        fail ArgumentError, 'timeout_ms cannot be nil'
      end

      if timeout_ms > 60000
        fail ArgumentError, 'invalid value for "timeout_ms", must be smaller than or equal to 60000.'
      end

      if timeout_ms < 1000
        fail ArgumentError, 'invalid value for "timeout_ms", must be greater than or equal to 1000.'
      end

      @timeout_ms = timeout_ms
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] format Object to be assigned
    def format=(format)
      validator = EnumAttributeValidator.new('String', ["png", "jpeg", "webp"])
      unless validator.valid?(format)
        fail ArgumentError, "invalid value for \"format\", must be one of #{validator.allowable_values}."
      end
      @format = format
    end

    # Custom attribute writer method with validation
    # @param [Object] quality Value to be assigned
    def quality=(quality)
      if quality.nil?
        fail ArgumentError, 'quality cannot be nil'
      end

      if quality > 100
        fail ArgumentError, 'invalid value for "quality", must be smaller than or equal to 100.'
      end

      if quality < 0
        fail ArgumentError, 'invalid value for "quality", must be greater than or equal to 0.'
      end

      @quality = quality
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          url == o.url &&
          wait_until == o.wait_until &&
          timeout_ms == o.timeout_ms &&
          viewport == o.viewport &&
          full_page == o.full_page &&
          format == o.format &&
          quality == o.quality &&
          selector == o.selector &&
          omit_background == o.omit_background
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [url, wait_until, timeout_ms, viewport, full_page, format, quality, selector, omit_background].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      attributes = attributes.transform_keys(&:to_sym)
      transformed_hash = {}
      openapi_types.each_pair do |key, type|
        if attributes.key?(attribute_map[key]) && attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = nil
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[attribute_map[key]].is_a?(Array)
            transformed_hash["#{key}"] = attributes[attribute_map[key]].map { |v| _deserialize($1, v) }
          end
        elsif !attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = _deserialize(type, attributes[attribute_map[key]])
        end
      end
      new(transformed_hash)
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def self._deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Unifapi.const_get(type)
        klass.respond_to?(:openapi_any_of) || klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
